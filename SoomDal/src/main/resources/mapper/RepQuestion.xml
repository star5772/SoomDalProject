<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="RepQuestionMapper">
	<select id="findAllToRequestion" resultType="RepQuestion">
		select d_q_no dQno, d_email dEmail, d_q_content dQcontent from repquestion where d_email=#{dEmail}
	</select>
	
	<insert id="insertToRequestion">
		insert into repquestion(d_q_no, d_email, d_q_content)
		values(#{dQno}, #{dEmail}, #{dQcontent})
	</insert>
</mapper>












