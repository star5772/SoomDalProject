<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.icia.dal.dao.QnaBoardDao">

	<insert id="insertToQnaBoard">
		<selectKey keyProperty="qNo" resultType="int"
			order="BEFORE">
			select qnaboard_seq.nextval from dual
		</selectKey>
		insert into QnaBoard values(#{qNo}, #{qTitle}, #{qContent}, sysdate,
		#{qWriter}, #{cNo}, 0,0)
	</insert>
	
	<select id="count" resultType="int">
		select count(*) from qnaBoard
		<where>
			<if test="qWriter != null">q_writer = #{qWriter}</if>
		</where>
	</select>
	
	<delete id="deleteToQnaBoard">
		delete from QnaBoard where Q_NO=#{qNo} and rownum=1
	</delete>
	
	<select id="findByQnaBoard" resultType="QnaBoard">
		select Q_NO qNo, Q_TITLE qTitle, Q_CONTENT qContent, Q_WRITER qWriter, Q_WRITE_DATE qWriteDate, C_NO cNo, Q_IS_SECRET qIsSecret, Q_IS_NOTICE qIsNotice from QnaBoard where Q_NO=#{qNo} and rownum=1
	</select>
	
	<select id="findAllByQnaBoard" resultType="QnaBoard">
 	<![CDATA[
 		select * from
 			select rownum as rnum, b.* from
				(select /*+ index_desc(qnaboard qnaboard_pk_qno) */ Q_NO,Q_TITLE,Q_WRITER,Q_WRITE_DATE from qnaboard) b
			where rownum>=#{endRowNum})
		where rnum>=#{startRowNum}
	]]> 
	</select>
	
	<select id="findAllByWriter" resultType="QnaBoard">
	 	<![CDATA[
	 		select * from
	 			select rownum as rnum, b.* from
					(select /*+ index_desc(qnaboard qnaboard_pk_qno) */ Q_NO,Q_TITLE,Q_WRITER,Q_WRITE_DATE from qnaboard where q_writer=#{qWriter}) b
				where rownum>=#{endRowNum})
			where rnum>=#{startRowNum}
		]]> 		
	</select>
</mapper>












