<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="detailFieldMapper">

	
	<!-- 달인 수 -->
	<select id="countToDalin" resultType="int">
		select count(*) 
		from dalin 
		where DETAIL_F_NAME=#{detailFName}
	</select>
	
	<select id="findByReview" resultType="review">
		select r_no rNo, r_date rDate, r_content rContent, r_score rScore, r_sajin rSajin, r_writer rWriter, s_no sNo, d_mno dMno , j_accusation_cnt jAccusationCnt
		from review
		where r_no=#{rNo} and rownum=1
	</select>
	
</mapper>












